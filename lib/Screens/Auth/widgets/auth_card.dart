import 'package:flutter/material.dart';
import 'auth_card_state.dart';

class AuthCard extends StatefulWidget {
  const AuthCard({
    Key key,
  }) : super(key: key);

  @override
  AuthCardState createState() => AuthCardState();
}
